#include <iostream>
#include <fstream>
using namespace std;
int main()
{
	setlocale(LC_ALL, "Rus");
	char s;
	cout << "В файле содержатся следующие цифры: \n";
	ifstream fin("file_3,4.txt");
	while (fin>>s)
	{
		if (s>=48 and s<=57) cout << s <<" ";
	}
	fin.close();
	return 0;
}
