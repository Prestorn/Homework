#include <iostream>
#include <fstream>
#include <Windows.h>
using namespace std;
int main()
{
	setlocale(LC_ALL, "Rus");
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	cout << "Введите текст, который вы хотите ввести в файл: " << endl;
	char s[256];
	cin.getline(s,256);
	ofstream fout("file_3,3.txt");
	fout << s;
	fout.close();
	ifstream fin("file_3,3.txt");
	fin.getline(s, 1000);
	fin.close();
	cout << "Текст \"" << s << "\" успешно записан в файл";
	return 0;
}
